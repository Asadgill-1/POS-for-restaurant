{
  "name": "@mizan/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "seed": "node src/seed.ts",
    "validate": "prisma validate",
    "typecheck": "tsc --noEmit",
    "lint": "prisma validate",
    "test": "echo \"no unit tests in @mizan/db — integration tests land in M1\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  },
  "devDependencies": {
    "prisma": "^6.3.1",
    "typescript": "^5.7.3",
    "@types/node": "^22.10.7"
  }
}
